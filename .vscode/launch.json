{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/out/debug.exe",
      "preLaunchTask": "Build Debug",
      "cwd": "${workspaceFolder}/out", 
    }
  ]
}